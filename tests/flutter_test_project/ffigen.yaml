# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterTestProjectBindings
description: |
  Bindings for `src/flutter_test_project.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_test_project_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_test_project.h'
  include-directives:
    - 'src/flutter_test_project.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
